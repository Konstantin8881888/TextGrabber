CREATE TABLE IF NOT EXISTS text_data (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         text TEXT NOT NULL,
                                         PRIMARY KEY (id)
);